import 'package:flutter/material.dart';

class NyraxColors {
  // ===== Tema Escuro =====
    static const Color darkBackground = Color(0xFF0A0F14);
      static const Color darkSurface = Color(0xFF11161D);
        static const Color darkCard = Color(0xFF151C24);

          // ===== Tema Claro =====
            static const Color lightBackground = Color(0xFFF5F7FA);
              static const Color lightSurface = Color(0xFFFFFFFF);
                static const Color lightCard = Color(0xFFF0F3F8);

                  // ===== Cores da Marca =====
                    static const Color cyan = Color(0xFF00E5FF);
                      static const Color pink = Color(0xFFFF2ED1);
                        static const Color purple = Color(0xFF7A2CFF);

                          // ===== Texto =====
                            static const Color darkText = Colors.white;
                              static const Color lightText = Color(0xFF111111);

                                // ===== Extras =====
                                  static const Color success = Color(0xFF00E676);
                                    static const Color warning = Color(0xFFFFC107);
                                      static const Color error = Color(0xFFFF3B3B);
                                      }